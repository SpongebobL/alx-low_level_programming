#include<stdio.h>

/**
 * main-entry point
 *declare variables i and pr
 *use putschar to output i
 *use puts to output pr
 *return 0
 */
int main(void)
{
	char i = '"';
	char pr[] = "Programming is like building a multilingual puzzle\n";
	putchar(i);
	puts(pr);
	return (0);
}
